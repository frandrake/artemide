---
import AppShell from '../../components/layout/AppShell.astro';
import BoardPipeline from '../../components/board/BoardPipeline';
---
<AppShell title="Board · Pipeline" current="board-pipeline" domain="board">
  <header class="bd-head">
    <h1>Board opportunities pipeline</h1>
    <p class="bd-subhead">Seven stages from surfaced to decision. Advancing past the conflict screen warns if not cleared.</p>
  </header>
  <BoardPipeline client:load />
</AppShell>

<style>
  .bd-head { margin-bottom: var(--space-4); display: flex; flex-direction: column; gap: var(--space-1); }
  .bd-subhead { color: var(--charcoal-muted); }
  .bd-head h1 { font-family: var(--font-serif); }
</style>
